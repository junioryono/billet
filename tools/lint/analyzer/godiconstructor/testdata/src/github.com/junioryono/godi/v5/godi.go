// Package godi is a stand-in for the real module, carrying only the one type
// the fixture names. The fixture must compile without the module cache.
package godi

// In marks a parameter object.
type In struct{}
